import styled from "styled-components";

import { CartItemContainer } from "../CartItem/index.styles";

export const UndefinedCartItemContainer = styled(CartItemContainer)`
  margin-bottom: 0;
`;
